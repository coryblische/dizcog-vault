services:
  - type: web
    name: dizcog-vault
    runtime: node
    plan: free
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm start
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
      - key: GITHUB_REPO
        value: coryblische/dizcog-vault
      - key: GITHUB_TOKEN
        sync: false
      - key: LEDGER_PASSWORD
        sync: false
      - key: LEDGER_AUTH_SECRET
        generateValue: true
